#include <iostream>
#include "Matrix.h"

using namespace std;

int main(){
    /* Matrix m1(5,8); // (Cols, Rows)
    cout << "First Matrix\n"; 
    m1.printMatrix();
    Matrix m2(m1.getMatrix(),m1.getColSize(), m1.getRowSize());
    cout << "Second Matrix\n";
    m2.printMatrix(); */
        

    return 0;
}